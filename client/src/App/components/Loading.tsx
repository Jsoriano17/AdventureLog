import { observer } from 'mobx-react-lite';
import React from 'react';

const Loading = () => {
    return (
        <div>
            Loading ...
        </div>
    )
}

export default observer(Loading);